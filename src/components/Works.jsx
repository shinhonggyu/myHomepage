import styled from 'styled-components';

const Container = styled.div`
  background-color: wheat;
`;

const Works = () => {
  return <Container id="works">Works</Container>;
};

export default Works;
